import { FileMap } from './file';
export declare type ResolveDepOptions = {
    entries: string[];
    cwd: string;
    expand: boolean;
    loadContent: boolean;
    files: FileMap;
};
export declare function normalizeOptions(args: Partial<ResolveDepOptions | string>[]): ResolveDepOptions;
